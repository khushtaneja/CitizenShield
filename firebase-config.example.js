/**
 * ============================================================
 *  firebase-config.example.js  —  CitizenShield SARS
 *
 *  Template file — safe to commit to version control.
 *
 *  HOW TO USE:
 *  1. Copy this file → firebase-config.js
 *  2. Replace each placeholder with your actual Firebase values
 *     (found in Firebase Console → Project Settings → Your Apps)
 *  3. firebase-config.js is gitignored and will never be committed.
 * ============================================================
 */

const firebaseConfig = {
  apiKey:            "YOUR_API_KEY",
  authDomain:        "YOUR_PROJECT_ID.firebaseapp.com",
  projectId:         "YOUR_PROJECT_ID",
  storageBucket:     "YOUR_PROJECT_ID.firebasestorage.app",
  messagingSenderId: "YOUR_MESSAGING_SENDER_ID",
  appId:             "YOUR_APP_ID",
};
